auto lo

iface lo inet loopback
iface eth0 inet dhcp

auto wlan0

allow-hotplug wlan0
iface wlan0 inet manual
iface wlan0 inet dhcp
wpa-conf /etc/wpa.conf

wpa-roam
/etc/wpa_supplicant/wpa_supplicant.conf
iface default inet dhcp




#create /etc/wpa.conf
sudo echo -e 'network={\n\tssid="Ap1 Network - wireless WiFi"\n\tkey_mgmt=WPA-PSK\n\tpsk="blackcomb1981"\n}' > /etc/wpa.conf
sudo ifup wlan0
